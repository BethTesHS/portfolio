:root {
  --text: #f1edea;
  --background: #1a181b; /* Slightly darker for better contrast */
  --primary: #b7aac5;
  --secondary: #5b4667;
  --accent: #a083aa;
  --card-bg: rgba(91, 70, 103, 0.15);
  --code-bg: #1e1e1e;
  --code-string: #ce9178;
  --code-keyword: #569cd6;
}

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  justify-content: center;
}

body {
  background-color: var(--background);
  color: var(--text);
  font-family: 'Inter', sans-serif;
  line-height: 1.6;
  overflow-x: hidden;
}

.portfolio {
  min-height: 100vh;
  position: relative;
}

/* Spotlight Effect logic */
.portfolio::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: radial-gradient(
    600px circle at var(--mouse-x) var(--mouse-y), 
    rgba(160, 131, 170, 0.06), 
    transparent 40%
  );
  z-index: -1;
  pointer-events: none;
}

.container {
  max-width: 1000px;
  margin: 0 auto;
  padding: 0 20px;
}

/* Dev Mode Toggle Button */
.dev-toggle {
  position: fixed;
  bottom: 30px;
  right: 30px;
  width: 50px;
  height: 50px;
  border-radius: 50%;
  background: var(--accent);
  color: #fff;
  border: none;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 15px rgba(0,0,0,0.3);
  z-index: 2000;
}

/* Navigation */
nav {
  position: fixed;
  width: 960px;
  background-color: var(--background);
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 2rem;
  padding-bottom: 20px;
  margin-bottom: 2rem;
}

.logo {
  font-size: 1.5rem;
  font-weight: 700;
  color: var(--accent);
  font-family: 'Fira Code', monospace; /* Monospace for logo */
}

.nav-links a {
  color: var(--primary);
  margin-left: 2rem;
  text-decoration: none;
  transition: color 0.3s ease;
  font-size: 0.95rem;
}

.nav-links a:hover {
  color: var(--text);
}

.btn {
  background-color: transparent;
  border: 1px solid var(--accent);
  color: var(--accent);
  padding: 0.5rem 1.2rem;
  border-radius: 4px;
}

.btn:hover {
  background-color: var(--accent);
  color: var(--text);
}

/* Hero */
.hero {
  padding: 8rem 0 4rem 0;
}

.terminal-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  background: rgba(0,0,0,0.3);
  padding: 6px 12px;
  border-radius: 20px;
  font-family: 'Fira Code', monospace;
  font-size: 0.8rem;
  color: var(--accent);
  margin-bottom: 1.5rem;
  border: 1px solid rgba(160, 131, 170, 0.2);
}

.hero h1 {
  font-size: 4rem;
  margin-bottom: 1.5rem;
  line-height: 1.1;
  font-weight: 800;
}

.gradient-text {
  background: linear-gradient(120deg, var(--text), var(--accent));
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
}

.hero p {
  font-size: 1.2rem;
  color: var(--primary);
  max-width: 600px;
  margin-bottom: 2rem;
}

/* Social Links */
.social-links {
  display: flex;
  gap: 1.5rem;
}

.social-icon {
  color: var(--primary);
}

/* Cards with Glow Border Effect */
.experience-grid {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.card {
  position: relative;
  background-color: var(--card-bg);
  border: 1px solid rgba(255, 255, 255, 0.05);
  border-radius: 12px;
  overflow: hidden;
  backdrop-filter: blur(5px);
}

.card-content {
  padding: 2rem;
  position: relative;
  z-index: 2;
}

/* Subtle glow behind cards */
.card-bg-glow {
  position: absolute;
  top: 0; left: 0; right: 0; bottom: 0;
  background: radial-gradient(
    circle at var(--mouse-x) var(--mouse-y),
    rgba(255, 255, 255, 0.05),
    transparent 80%
  );
  z-index: 1;
  opacity: 0;
  transition: opacity 0.3s;
}

.card:hover .card-bg-glow {
  opacity: 1;
}

.card-header h3 {
  color: var(--text);
  font-size: 1.25rem;
}

.company {
  color: var(--accent);
  font-family: 'Fira Code', monospace;
  font-size: 0.9rem;
}

.date {
  color: var(--primary);
  font-size: 0.85rem;
}

ul {
  list-style: none;
  margin-top: 1rem;
}

li {
  position: relative;
  padding-left: 1.5rem;
  margin-bottom: 0.5rem;
  color: var(--primary);
}

li::before {
  content: "▹";
  position: absolute;
  left: 0;
  color: var(--accent);
}

/* Skills */
.skills-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
  gap: 1rem;
}

.skill-tag {
  background: rgba(255,255,255,0.05);
  color: var(--text);
  padding: 0.8rem;
  text-align: center;
  border-radius: 6px;
  font-size: 0.9rem;
  cursor: default;
  border: 1px solid transparent;
  transition: border-color 0.3s;
}

.skill-tag:hover {
  border-color: var(--accent);
}

/* JSON View Styles (The Gimmick) */
.json-view {
  margin-top: 4rem;
  background: var(--code-bg);
  padding: 2rem;
  border-radius: 10px;
  font-family: 'Fira Code', monospace;
  color: #d4d4d4;
  box-shadow: 0 10px 30px rgba(0,0,0,0.5);
  border: 1px solid #333;
  overflow-x: auto;
}

.json-header {
  display: flex;
  gap: 8px;
  margin-bottom: 1.5rem;
  padding-bottom: 1rem;
  border-bottom: 1px solid #333;
  align-items: center;
}

.dot { width: 12px; height: 12px; border-radius: 50%; }
.red { background: #ff5f56; }
.yellow { background: #ffbd2e; }
.green { background: #27c93f; }
.filename { margin-left: 10px; color: #888; font-size: 0.8rem; }

pre {
  white-space: pre-wrap;
  font-size: 0.9rem;
}


/ ----------------------------------------------------------------------------------------------------------------------------


import React, { useState, useEffect, useRef } from 'react';
import './App.css';
import { motion, AnimatePresence } from 'framer-motion';
import { 
  Mail, Phone, MapPin, Github, Linkedin, 
  ExternalLink, Code, Database, Layout, 
  Terminal, FileJson, Eye 
} from 'lucide-react';

const Portfolio = () => {
  const [showJson, setShowJson] = useState(false);
  const [mousePosition, setMousePosition] = useState({ x: 0, y: 0 });
  const containerRef = useRef(null);

  // Gimmick: Mouse tracking for spotlight effect
  useEffect(() => {
    const handleMouseMove = (e) => {
      if (containerRef.current) {
        const rect = containerRef.current.getBoundingClientRect();
        setMousePosition({
          x: e.clientX - rect.left,
          y: e.clientY - rect.top,
        });
      }
    };
    window.addEventListener('mousemove', handleMouseMove);
    return () => window.removeEventListener('mousemove', handleMouseMove);
  }, []);

  const personalInfo = {
    name: "Bethelhem Tesfaye Haileselassie",
    role: "Software Engineering",
    email: "bethelhemtesfaye95@gmail.com",
    phone: "(+254) 115-190-303",
    location: "Nairobi, Kenya",
    linkedin: "https://www.linkedin.com/in/bethelhem-tesfaye-b26277272",
    github: "https://github.com/BethTesHS",
  };

  const skills = [
    "C#", "Java", "Dart", "Flutter", "Laravel", 
    "Python", "CSS", "JavaScript", "MySQL", 
    "Azure", "Git", "Figma",  
  ];

  const experience = [
    {
      role: "Software Engineering Intern",
      company: "Microsoft",
      location: "Nairobi, Kenya",
      date: "July 2025 - September 2025",
      points: [
        "Worked with the IDNA team to design core components of an AI-powered diagnostic chatbot.",
        "Integrated Azure OpenAI and RAG for intelligent error analysis.",
        "Implemented secure authentication using Microsoft Entra ID.",
        "Optimized Kusto query services to analyze telemetry logs."
      ]
    },
    {
      role: "Web Application Developer",
      company: "Strathmore University (Lost & Found Project)",
      location: "Nairobi, Kenya",
      date: "April 2025 - November 2025",
      points: [
        "Developed a full-stack Laravel web app for item management.",
        "Engineered a RAG search endpoint using Gemini AI for image-to-text conversion.",
        "Built real-time messaging using WebSockets (Reverb/Pusher).",
        "Integrated geospatial mapping and QR code generation."
      ]
    },
    // ... other jobs
  ];

  // Animation Variants
  const containerVariants = {
    hidden: { opacity: 0 },
    visible: { 
      opacity: 1,
      transition: { staggerChildren: 0.1 }
    }
  };

  const itemVariants = {
    hidden: { y: 20, opacity: 0 },
    visible: { y: 0, opacity: 1 }
  };

  return (
    <div 
      className={`portfolio ${showJson ? 'json-mode' : ''}`} 
      ref={containerRef}
      style={{
        '--mouse-x': `${mousePosition.x}px`,
        '--mouse-y': `${mousePosition.y}px`
      }}
    >
      {/* Dev Mode Toggle Gimmick */}
      <motion.button 
        className="dev-toggle"
        onClick={() => setShowJson(!showJson)}
        whileHover={{ scale: 1.1 }}
        whileTap={{ scale: 0.9 }}
        title="Toggle Developer Mode"
      >
        {showJson ? <Eye size={20} /> : <FileJson size={20} />}
      </motion.button>

      <AnimatePresence mode="wait">
        {showJson ? (
          // JSON VIEW MODE (The "Developer View")
          <motion.div 
            key="json"
            initial={{ opacity: 0, scale: 0.95 }}
            animate={{ opacity: 1, scale: 1 }}
            exit={{ opacity: 0, scale: 1.05 }}
            className="json-view container"
          >
             <div className="json-header">
                <span className="dot red"></span>
                <span className="dot yellow"></span>
                <span className="dot green"></span>
                <span className="filename">resume.json</span>
             </div>
             <pre>
               <code>
{`{
  "name": "${personalInfo.name}",
  "role": "${personalInfo.role}",
  "contact": {
    "email": "${personalInfo.email}",
    "github": "${personalInfo.github}"
  },
  "skills": ${JSON.stringify(skills, null, 4)},
  "experience": [
    ${experience.map(exp => `{
      "role": "${exp.role}",
      "company": "${exp.company}",
      "stack": "Laravel, AI, Azure"
    }`).join(',\n    ')}
  ]
}`}
               </code>
             </pre>
          </motion.div>
        ) : (
          // STANDARD VIEW MODE
          <motion.div 
            key="standard"
            className="container"
            variants={containerVariants}
            initial="hidden"
            animate="visible"
            exit={{ opacity: 0, y: -20 }}
          >
            <nav>
              <motion.div 
                className="logo"
                whileHover={{ rotate: -5, scale: 1.1 }}
              >
                &lt;BethTesHS /&gt;
              </motion.div>
              <div className="nav-links">
                <a href="#about">About</a>
                <a href="#experience">Experience</a>
                <a href="#skills">Skills</a>
                <motion.a 
                  href={`mailto:${personalInfo.email}`} 
                  className="btn"
                  whileHover={{ scale: 1.05 }}
                  whileTap={{ scale: 0.95 }}
                >
                  Contact Me
                </motion.a>
              </div>
            </nav>

            <header className="hero">
              <motion.div variants={itemVariants}>
                <div className="terminal-badge">
                  <Terminal size={14} /> 
                  <span>bethelhem@portfolio:~$ ./welcome.sh</span>
                </div>
                <h1>
                  Hello, I'm <br />
                  <span className="gradient-text">{personalInfo.name}</span>
                </h1>
              </motion.div>
              
              <motion.p variants={itemVariants}>
                {personalInfo.role} based in {personalInfo.location}. 
                Specializing in building robust web and mobile applications with 
                modern technologies like React, Laravel, and AI integrations.
              </motion.p>
              
              <motion.div className="social-links" variants={itemVariants}>
                {[
                  { icon: Mail, href: `mailto:${personalInfo.email}` },
                  { icon: Linkedin, href: personalInfo.linkedin },
                  { icon: Github, href: personalInfo.github },
                ].map((social, i) => (
                  <motion.a 
                    key={i} 
                    href={social.href} 
                    className="social-icon" 
                    whileHover={{ y: -5, color: 'var(--accent)' }}
                  >
                    <social.icon size={24} />
                  </motion.a>
                ))}
              </motion.div>
            </header>

            <section id="experience">
              <motion.h2 className="section-title" variants={itemVariants}>
                Experience
              </motion.h2>
              <div className="experience-grid">
                {experience.map((job, index) => (
                  <motion.div 
                    key={index} 
                    className="card"
                    variants={itemVariants}
                    whileHover={{ scale: 1.02 }}
                  >
                    <div className="card-bg-glow"></div>
                    <div className="card-content">
                      <div className="card-header">
                        <div>
                          <h3>{job.role}</h3>
                          <span className="company">{job.company}</span>
                        </div>
                        <span className="date">{job.date}</span>
                      </div>
                      <ul>
                        {job.points.map((point, i) => (
                          <li key={i}>{point}</li>
                        ))}
                      </ul>
                    </div>
                  </motion.div>
                ))}
              </div>
            </section>

            <section id="skills">
              <motion.h2 className="section-title" variants={itemVariants}>
                Technical Skills
              </motion.h2>
              <div className="skills-grid">
                {skills.map((skill, index) => (
                  <motion.div 
                    key={index} 
                    className="skill-tag"
                    variants={itemVariants}
                    whileHover={{ scale: 1.1, backgroundColor: "var(--accent)" }}
                  >
                    {skill}
                  </motion.div>
                ))}
              </div>
            </section>
            
            <footer style={{ marginTop: '4rem', paddingBottom: '2rem', textAlign: 'center', color: 'var(--primary)' }}>
               <p>© {new Date().getFullYear()} BethTesHS. Built with React & Coffee.</p>
            </footer>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
};

export default Portfolio;